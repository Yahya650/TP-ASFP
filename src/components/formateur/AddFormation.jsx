import React from 'react'
import { useContextApi } from '../../context/ContextApi';

const AddFormation = () => {

    const { addFormation, fetchFormations } = useContextApi();

    return (
        <div>
            
        </div>
    )
}

export default AddFormation
